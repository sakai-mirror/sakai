##<!-- $Header: /cvs/sakai/chef-tool/src/webapp/vm/sitesetupgeneric/chef_site-importSites.vm,v 1.11 2005/02/14 18:14:21 zqian.umich.edu Exp $ -->
<div class="portletBody">
	<h3 class="insColor insBak insBor">
		Re-use Material from Other Sites
	</h3>
	<p class="step">
		Step $!step of $!totalSteps: Re-use material from other sites you own...
	</p>
	<p class="instruction">
		Choose the material you want to re-use from these sites. You can combine material from  more than one site (for example, Resources from several sites).
	</p>
	<form name="importSitesForm" action="#toolForm("$action")" method="post">
		<table class ="listHier" cellspacing="0" summary="import sites and their tools" border="0">
			<tr>
				<th>
					&nbsp;
				</th>
				#foreach($site in $importSites.keys())
					<th>
						$site.getTitle()
					</th>
				#end
			</tr>
			#foreach($toolId in $selectedTools)
				#if ($toolId == "chef.schedule" || $toolId == "chef.announcements" || $toolId == "chef.resources" || $toolId == "chef.discussion" || $toolId == "chef.assignment")
					<tr>
						<td valign="top" align="center">
							#set($toolTitle = "")
							#foreach($t in $toolRegistrationList)
								#if ($t.getId() == $toolId)
									#set($toolTitle = $t.getTitle())
								#end
							#end
							$toolTitle
						</td>
						#foreach($site in $importSites.keys())
							<td align="center">
								#set($toolFound = false)
								#set($allPages = $site.getPages())
								#foreach ($page in $allPages)
									#set($pageTools = $page.getTools())
									#foreach ($pageTool in $pageTools)
										#if ($pageTool.ToolId == $toolId)
											#set($toolFound = true)
										#end
									#end
								#end
								#if ($toolFound)
									<input type="checkbox" name="$toolId" value="$site.Id" />
								#else
									<input type="checkbox" name="$toolId" value="$site.Id" disabled="disabled" />
								#end
							</td>
						#end
					</tr>
				#end
			#end
		</table>
		<input type="hidden" name="back" value="$back" />
		<input type="hidden" name="template-index" value="27" />
		<input type="hidden" name="continue" value="$continue" />
		<p class="act">
			<input type="submit" class="active" name="eventSubmit_doContinue" value="Continue" />
			<input type="submit" name="eventSubmit_doBack" value="Back" />
			<input type="submit" name="eventSubmit_doCancel" value="Cancel" />
		</p>
	</form>
</div>
